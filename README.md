# DNS Anomaly Detection System
A machine learning-based DNS anomaly detection system built with Python.

## Components
- Isolation Forest ML model for anomaly detection
- Adaptive model for dynamic threshold adjustment
- Real-time DNS query monitoring
- Web dashboard built with Streamlit

## Usage
Run the detector: python detector.py
Launch dashboard: streamlit run app.py
